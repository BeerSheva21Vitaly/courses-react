import { Typography } from "@mui/material";
import React from "react";

const Logout: React.FC = () => {
    return <Typography variant="h1" component="h1">Logout works</Typography>
}

export default Logout;