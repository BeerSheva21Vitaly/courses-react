import { Typography } from "@mui/material";
import React from "react";

const Courses: React.FC = () => {
    return <Typography variant="h1" component="h1">Courses works</Typography>
}

export default Courses;