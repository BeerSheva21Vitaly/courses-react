import { Typography } from "@mui/material";
import React from "react";

const AddCourse: React.FC = () => {
    return <Typography variant="h1" component="h1">Add Course works</Typography>
}

export default AddCourse;